selected.forEach((_, index) => [...selected].splice(index).reduce((a, b, _, arr) => !arr.includes((a + b) / 2) ? a : console.log('win')));

a.find(arr => arr.filter(element => ![1,3,5,4].includes(element)))


